# Grid Search Configuration for Rustrade Optimizer
# Regime: Bull Market Detection (AAPL Nov-Dec 2024)

# Corrected Schema for ParameterGrid struct

# Fast SMA: Look for quicker entries (10-30) vs standard 20
fast_sma = [10, 15, 20, 25, 30]

# Slow SMA: Trend filter.
slow_sma = [100, 150, 200]

# RSI Threshold: Testing higher thresholds for bull market
rsi_threshold = [60.0, 65.0, 70.0, 75.0]

# Trend Divergence: How strict on trend alignment?
trend_divergence_threshold = [0.002, 0.005, 0.010]

# Trailing Stop ATR Multiplier
trailing_stop_atr_multiplier = [2.5, 3.0, 3.5, 4.0]

# Order Cooldown
order_cooldown_seconds = [60, 300]
